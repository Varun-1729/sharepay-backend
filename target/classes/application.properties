# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/splitwise_clone?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Varun@1729
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Jackson JSON Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false

# Basic Security
spring.security.user.name=splitwise
spring.security.user.password=secure123
spring.security.user.roles=USER

# Server Configuration
server.port=8080

# Logging
logging.level.com.splitwise=DEBUG
logging.level.org.springframework.web=DEBUG
